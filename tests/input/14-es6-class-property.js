/**
 * @typedef {Object} StreamDB
 * @property {function(*):Promise<Readable>} stream
 * @memberOf module:model
 */

/*::
 const { A } = require("A").A;
*/

/**
 * @property {string} one
 * @property {number} two
 */
class Foo {
}
